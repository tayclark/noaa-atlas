# NOAA Atlas

A linked globe (NOAA APIs in action) and graph (the NOAA API ecosystem) that answers "which NOAA API do I use for X?".

```sh
npm install
npm run dev     # local dev server
npm test        # unit tests
npm run build   # typecheck + production build
```
